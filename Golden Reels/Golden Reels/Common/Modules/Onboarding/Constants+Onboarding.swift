import UIKit

enum Constans { }

extension Constans {
    enum Onboarding {
        static let items: [OnboardingItemViewModel] = [
            .init(
                image: .init(named: "рис")
            ),
            .init(
                image: .init(named: "рис1")
            ),
            .init(
                image: .init(named: "рис2")
            ),
            .init(
                image: .init(named: "рис3")
            )
        ]
    }
}
